# Runtime Contract Audit (2026-02-06_18-44-05)

## 审计目标
- 运行态契约：`/random` `/i` `/images` `/healthz` `/metrics` `/admin` + legacy 路由
- compose 可部署性：`backend` / `migrate` / `postgres` / `memcached`

## 审计环境
- 本地启动：`node dist/app.js`
- 临时运行参数（仅运行时）：
  - `PORT=3015`
  - `MEMCACHED_HOST=127.0.0.1`
  - `MEMCACHED_PORT=11211`
  - `MEMCACHED_NAMESPACE=pixivcat`
  - `DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/pixivcat?schema=public`
- 审计证据：
  - `docs/review/2026-02-06_18-44-05-runtime-contract-check.json`
  - `docs/review/2026-02-06_18-44-05-runtime-server.out.log`
  - `docs/review/2026-02-06_18-44-05-runtime-server.err.log`

## API / 路由契约结果
| 路径 | HTTP | 结果摘要 | 判定 |
|---|---:|---|---|
| `/healthz` | 503 | 返回 JSON，包含 `ok/db/memcached/queue` 结构；提示 DB/Memcached 未就绪 | 契约结构正确（依赖受限） |
| `/metrics` | 200 | Prometheus 文本输出，包含 `pixivcat_up/http_requests_total/outbound_errors_total` | 通过 |
| `/random?format=json` | 500 | JSON 错误体含 `code/message/request_id`（示例 `P1001`） | 契约结构正确（依赖受限） |
| `/random?redirect=1` | 500 | 在依赖未就绪下按预期返回 500 分支 | 可接受降级 |
| `/i/1.jpg` | 500 | HTML 错误页（legacy 样式） | 路由存在，依赖不足 |
| `/images/1` | 500 | JSON 错误体 | 路由存在，依赖不足 |
| `/admin`（未鉴权） | 401 | 返回 `UNAUTHORIZED` JSON | 保护面有效 |
| `/12345.jpg` | 500 | legacy 单图路由可达但上游依赖失败 | 路由存在 |
| `/12345-2.jpg` | 500 | legacy 多页路由可达但上游依赖失败 | 路由存在 |

## 脚本化回归结果
- `pwsh test/proxy-smoke.ps1 -BaseUrl http://127.0.0.1:3015`：通过（允许 503/500 分支）。
- `pwsh test/admin-ui-smoke.ps1 -BaseUrl http://127.0.0.1:3015`：通过（鉴权页与 page API 可访问）。

## compose 可部署性审计
- `docker compose config --services`：通过，服务为 `memcached/postgres/migrate/backend`。
- `docker compose config --quiet`：通过（配置语法可解析）。
- `docker compose up -d`：受限（命令超时，当前 shell 无法确认 daemon 可用）。
- `docker info` / `docker version`：受限（均超时）。

## 结论
- **契约层面**：关键路由均可达，错误分支结构基本一致；在 DB/缓存缺失时表现为可解释降级。
- **运行态可用性**：当前环境缺少 Postgres/Memcached，导致核心数据路径与 Admin 资源列表大量 500（`P1001`）。
- **部署性**：compose 文件结构完整，但在本终端环境未能完成 daemon 级启动验证。
