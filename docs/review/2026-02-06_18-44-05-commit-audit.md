# Commit Audit (2026-02-06_18-44-05)

## 审计范围与方法
- 命令：`git log --reverse --date=iso --pretty=format:"%h|%ad|%an|%s"`
- 抽样深挖：`git show --name-status --stat`（关键里程碑提交 + 非 CSV 映射提交）
- 对照对象：
  - `issues/2026-01-31_19-36-13-random-anime-image-api.csv`
  - `issues/2026-02-05_01-07-40-proxy-pool-token-binding-hot-reload.csv`
  - `issues/2026-02-06_06-20-55-full-refactor-and-ux-overhaul.csv`

## 全量统计
- 总提交数：`322`
- 按 CSV ID 映射：`RAPI=226`，`PPOOL=65`，`FRUX=12`
- 非 CSV 映射提交：`19`
  - 含首提、回归状态 meta 提交、若干热修（Admin bundle、docker env、TS strict）

## 关键提交审计（提交 -> 文件 -> 功能 -> 风险 -> 测试覆盖 -> 结论 -> CSV映射）
| Commit | 关键文件 | 功能变化 | 风险评估 | 测试覆盖线索 | 结论 | CSV映射 |
|---|---|---|---|---|---|---|
| `ad6abf7` | `app.js`, `src/routes/pixivRoutes.js`, `src/services/*` | 项目基线与 legacy 代理能力初始落地 | 中：早期 JS 单体结构，后续易分叉 | 初始阶段无系统化证据 | 基线可追溯 | 无（首提） |
| `5c2cb07` | `app.ts`, `src/routes/{random,images,imageById,metrics,healthz}.ts` | 新 API 聚合路由进入主线 | 中：核心入口改动半径大 | 后续 RAPI 契约测试逐步补齐 | 架构拐点提交 | `RAPI-0041` |
| `2474129` | `src/middlewares/adminAuth.ts`, `src/routes/admin.ts` | 后台 ADMIN_TOKEN 鉴权建立 | 中：鉴权策略正确性直接影响暴露面 | `test/admin_auth.test.ts` 后续覆盖 | 为 Admin 面打基础 | `RAPI-0082` |
| `7b156c9` | `src/admin/adminJs.ts`, `prisma/schema.prisma`, `package.json` | AdminJS 体系接入，资源管理引入 | 高：UI 资源与 DB 绑定，运行态依赖增多 | 后续 Admin 测试分批补齐 | 功能大幅扩展 | `RAPI-0084` |
| `58ec9c3` | `src/config/runtimeConfig.ts`, `src/admin/adminJs.ts` | 运行时热更新缓存失效机制 | 中：并发下 stale cache 与一致性风险 | `test/hotReload.test.ts` | 方向正确 | `PPOOL-0506` |
| `f0a1242` | `src/admin/pages/easyProxiesImport.jsx`, `src/proxy/easyProxiesImporter.ts` | easy_proxies 导入/刷新动作接入后台 | 高：UI-Action-后端链路耦合，易出现组件/打包回归 | `npm run test:all` + importer 单测 | 功能进入可用态 | `PPOOL-0505` |
| `4b7d29a` | `src/proxy/proxyUriImporter.ts`, `src/proxy/easyProxiesImporter.ts` | URI 导入解析逻辑抽离服务化 | 中：迁移期兼容性风险 | `test/proxyUriImporter.test.ts` | 重构方向正确 | `FRUX-0002` |
| `636fb9c` | `src/admin/pages/easyProxiesImport.jsx` | 一行 URI / 多行导入 UI 增强 | 中：前端页面与组件注册同步风险 | `test/proxyUri.test.ts` + importer tests | 目标功能覆盖但运行态仍有裂缝 | `FRUX-0004` |
| `57285f6` | `src/admin/pages/uiKit.js`, `dashboard.jsx`, `importUrls.jsx` | UI token/视觉统一 ADR 落地 | 中：样式统一不等于组件链路正确 | lint/build 证据存在，运行态页面巡检不足 | 文档与代码有进展 | `FRUX-0005` |
| `ede16af` | `src/routes/{images,tags,authors}.ts`, `src/metrics/classificationMetrics.ts` | 分类接口契约与指标补齐 | 中：契约变更需持续回归保护 | `test/apiClassification.test.ts`, `test/metrics.test.ts` | 后端契约增强有效 | `FRUX-0008` |
| `b8bd742` | `issues/2026-02-06_06-20-55-full-refactor-and-ux-overhaul.csv` | 最终回归状态收口 | 中：回归结果依赖运行环境完整度 | `npm run test:all` + smoke | 状态闭环完成 | `FRUX-0011` |
| `ec6757c` | `src/admin/adminJs.ts`, `src/proxy/easyProxiesImporter.ts` 等 | TS strict 构建阻塞热修 | 中：关键热修未映射到 issue，追踪性下降 | 构建通过（历史说明） | 必要热修但治理上有缺口 | 无（未映射） |

## 非映射提交风险
- 非映射提交 19 条中，除 meta 回归状态更新外，存在真实行为变更提交（例如 `0601b45`、`bfc7a9d`、`7ed8335`、`b524d0d`、`a6c5f79`、`ab9ac53`、`137099d`、`ec6757c`）。
- 其中多条与 Admin 组件打包、导入能力、docker env 解析相关，均属于高影响面路径。
- 结论：**提交可追溯性总体高（按 issue 映射完备），但热修未回填 CSV 的治理缺口客观存在**。

## 与历史 CSV 的映射关系结论
- 三份历史 CSV 的每个 `ID` 均可在提交历史中找到对应 `[ID]` 提交（抽样脚本验证 missing=0）。
- 风险不在“ID 找不到提交”，而在“提交存在但未进入当期 CSV 契约”——这会降低审计闭环可解释性。
- 建议：后续所有行为变更提交（包括 hotfix）均补 1 条 issue（即使是 `HOTFIX-*`）。
