# ADM-0004 — ProxyPool 资源页补齐（只读 list/show）+ 入口统一（修复验证）

## 线上复现（修复前）
- Admin 导航缺失 ProxyPool 资源（仅有 proxyPoolOverview 自定义页）：见本轮 Admin 深测记录

## 修复点（代码定位）
- `src/admin/resources/proxyPools.ts`：新增 ProxyPool 资源（只读 list/show）
- `src/admin/adminJs.ts`：注册资源并在导航中暴露入口
- `src/admin/pages/proxyPoolOverview.jsx`：与资源页互链（避免“只能看概览不能审计/定位”）

## 本地验证（已通过）
- `npx vitest run test/proxyPool.test.ts test/admin_resource_route_compat.test.ts`

## 线上复验步骤（需 redeploy 后执行）
1) Chrome MCP 打开：`/admin/resources/ProxyPool`（list/show）
2) 从 `proxyPoolOverview` 与 `ProxyPool` 资源页互相跳转，确认入口一致

