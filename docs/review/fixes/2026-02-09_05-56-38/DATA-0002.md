# DATA-0002 — 大量 URL 导入健壮性 + 审计可回滚（修复验证）

## 线上复现证据（修复前）
- 导入请求在大输入下出现 Cloudflare 504（控制台记录）：`docs/review/console/2026-02-08_04-48-29/admin_actions_console_messages_011.txt`
- 动作 POST 检查清单包含 `POST /admin/images/import -> 504`：`docs/review/network/2026-02-08_04-48-29/admin_action_post_checks.txt`

## 修复点（代码定位）
- `src/routes/adminImport.ts:283`：`POST /admin/images/import` 改为“解析/去重 → 创建 Import → enqueue job → 立即返回 import_id/job_id”
- `src/routes/adminImport.ts:167`：`GET /admin/imports/:id` 返回 progress（含 deduped）
- `src/routes/adminImport.ts:220`：`POST /admin/imports/:id/rollback` 入队回滚 job（disable/delete）
- `src/jobs/importImages.ts:105`：`admin_images_import` job：写库（bulk/单条）+ 按策略 enqueue hydrate_metadata + 更新 Import.detail
- `src/jobs/importImages.ts:273`：`admin_import_rollback` job：按 `images.created_import_id` 批量禁用/删除并写入 Import.detail.rollback
- `src/services/import/imageWriteService.ts:24`：bulk upsert 写入 `created_import_id`（仅 create，不覆盖已有记录）
- `prisma/schema.prisma:49` + `prisma/migrations/20260209072000_add_images_created_import_id/migration.sql`：新增 `images.created_import_id` + 索引
- `src/admin/pages/importUrls.jsx:167`：UI 增加“刷新进度 / 回滚（禁用）”入口

## 本地验证（已通过）
- `npx vitest run test/admin_import.test.ts`
- `npx vitest run test/imports_repo.test.ts test/images_repo.test.ts`
- `npm run lint`
- `npm run build`

## 线上复验步骤（需 redeploy + migrate 后执行）
1) Chrome MCP 打开：`/admin/pages/importUrls`
2) Preview（包含空行/重复/非法/超长行）
   - 期望：返回 `dry_run=true`；errors 逐行定位；`error_export` 可下载/复制（不写库）
3) Import（大批量输入）
   - 期望：快速返回 `import_id/job_id`（queued=true），不触发 504
4) Progress
   - 触发 `GET /admin/imports/:id`
   - 期望：progress.processed 递增，done 最终为 true；queue 字段可解释
5) Rollback
   - 触发 `POST /admin/imports/:id/rollback`（mode=disable 或 mode=delete）
   - 期望：返回 job_id；随后 `GET /admin/imports/:id` 的 `import.detail.rollback.affected > 0`（对“本次新增”的图片）

