# DEP-0001 — 暴露 build/version/commit 并修复部署一致性（修复验证）

## 线上复现证据（修复前）
- `/healthz` 缺少 build 字段（脱敏摘要）：`docs/review/network/2026-02-09_05-56-38/api-E1-healthz.body.txt`
- `/version` 404（headers 摘要）：`docs/review/network/2026-02-09_05-56-38/api-E2-version.headers.txt`

## 修复点（代码定位）
- `src/utils/buildInfo.ts`：build/version/commit/build_time 生成与缓存
- `src/routes/healthz.ts`：`/healthz` 返回包含 `build.*`
- `src/routes/version.ts`：新增 `/version` 返回同 build 信息
- `src/admin/pages/dashboard.jsx`：Dashboard 展示 build 信息（用于线上定位“版本不一致/旧构建”）

## 本地验证（已通过）
- `npx vitest run test/healthz.test.ts test/smoke.test.ts`

## 线上复验步骤（需 redeploy 后执行）
1) `curl https://i.mukyu.ru/healthz`
   - 期望：body 包含 `build.version / build.commit / build.build_time`（不含任何敏感信息）
2) `curl https://i.mukyu.ru/version`
   - 期望：同上（用于排障与缓存绕过）
3) Admin Dashboard
   - 期望：可直接看到 build commit，与仓库 `test` 分支 HEAD 对齐

