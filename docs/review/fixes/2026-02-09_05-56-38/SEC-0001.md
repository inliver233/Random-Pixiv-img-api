# SEC-0001 — ProxyEndpoint 密码 write-only + 全链路脱敏（修复验证）

## 线上复现证据（修复前）
- Edit 页密码回显（已脱敏截图）：`docs/review/screenshots/2026-02-09_05-56-38/admin-B1-proxyEndpoint-1-edit-password-visible-masked.png`
- Admin API 返回 `record.params.password`（脱敏摘要）：`docs/review/network/2026-02-09_05-56-38/admin-B1-proxyEndpoint-password-visible.summary.txt`

## 修复点（代码定位）
- `src/admin/resources/proxyEndpoints.ts:641`：actions response sanitize（write-only：不向客户端返回已存 password）
- `src/admin/resources/proxyEndpoints.ts:684`：edit.before 处理占位符/空值（留空=不修改）
- `src/admin/resources/proxyEndpoints.ts:790`：properties.password `type=password` + placeholder（UI 不回显）
- `src/audit/adminAudit.ts:42`：审计 detail 递归 redaction（包含 password/refresh_token/authorization/cookie）

## 本地验证（已通过）
- `npx vitest run test/admin_audit_redaction.test.ts test/admin_proxy_endpoint_writeonly.test.ts test/admin_proxy_probe_action.test.ts`

## 线上复验步骤（需 redeploy 后执行）
1) Chrome MCP 打开：`/admin/resources/ProxyEndpoint/records/1/edit`
   - 期望：密码 input `type=password`；不回显已存 password（仅占位，留空=不修改）
2) 抓取：`/admin/api/resources/ProxyEndpoint/records/1/edit`
   - 期望：响应体不包含 `record.params.password`
3) 触发 `ProxyEndpoint.probe`
   - 期望：notice / network / console 不出现明文 proxy 密码（仅允许 `***` / `[REDACTED]` 类掩码）

