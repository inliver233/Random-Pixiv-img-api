# ADM-0002 — Admin actions 异步 job 化（避免 504）+ adminJobs（修复验证）

## 线上复现证据（修复前）
- 同步动作触发 Cloudflare 504：`docs/review/network/2026-02-09_05-56-38/admin-A3-image-hydrateMetadata-post-504.summary.txt`

## 修复点（代码定位）
- `src/jobs/adminActions.ts`：Admin actions 入队 + timeout guard（返回 jobId，不同步阻塞）
- `src/admin/pages/adminJobs.jsx`：作业列表/状态/取消/重试（可观测与恢复入口）
- `src/admin/resources/images.ts` / `src/admin/resources/pixivTokens.ts` / `src/admin/resources/proxyEndpoints.ts`：actions handler 改为 enqueue

## 本地验证（已通过）
- `npx vitest run test/admin_action_component_contract.test.ts test/pixivTokenResource.test.ts test/admin_proxy_probe_action.test.ts`

## 线上复验步骤（需 redeploy 后执行）
1) Chrome MCP 触发 actions：
   - `Image.hydrateMetadata`、`PixivToken.testRefresh`、`ProxyEndpoint.probe`
   - 期望：<10s 返回 `success` notice 且包含 `jobId`（不再 504）
2) 打开 `/admin/pages/adminJobs`
   - 期望：可查看 job 状态，并可取消/重试；失败原因可解释（timeout/blocked/上游限流等）

