# 第二次最详细页面和功能实现问题（全量审计 → Issue 化 → 修复执行）

> 目标站点：`https://i.mukyu.ru`  
> 结论先行：**线上仍为旧部署（未 redeploy）**，因此你在网页上看到的“大部分不可用/导入 success=0/动作 not found”等，属于“线上旧版本现象”。仓库 `test` 分支已累计多笔修复提交，但**必须完成 redeploy + migrate** 后才能在 `i.mukyu.ru` 上复验并关闭回归状态（否则不得伪造通过）。

---

## 1. 审计基线摘要（我理解的“应实现能力”）

输入基线（已读并用于对照）：
- `E:/pixiv-download-修改版本/最初计划.txt`
- `pixiv-反代/pixivcat-backend/随机api开发规划.md`
- `pixiv-反代/pixivcat-backend/README.md`
- `pixiv-反代/pixivcat-backend/docs/**`（重点：`docs/admin.md`、`docs/api/*`、`docs/errors.md`、`docs/queue.md`、`docs/traceability-proxy-pool.md`、`docs/deployment/*`）
- `pixiv-反代/pixivcat-backend/plan/*`、`pixiv-反代/pixivcat-backend/issues/*`
- 真实站点审计证据（旧部署复现，已落盘）：
  - `docs/review/2026-02-09_05-56-38-admin-deep-audit.md`
  - `docs/review/2026-02-09_05-56-38-api-contract-audit.md`
  - `docs/review/screenshots/2026-02-09_05-56-38/`
  - `docs/review/network/2026-02-09_05-56-38/`

你对项目的本质需求（工程化表述）：
1) **导入大量图片**：你提供大量 Pixiv `pximg` 原图 URL（可达 1e5+ 行级别），系统必须能稳定接收、去重、入库，并可追溯（Import 记录 / jobId / 进度 / 回滚）。
2) **refreshToken + 代理池驱动冷路径补全**：使用 refreshToken 获取 accessToken，结合代理池出站，批量补全作者/标签/R18/尺寸等关键元数据；失败要可解释、可重试、可熔断限速。
3) **基于元数据分类检索**：`/tags` `/authors` `/images` 能检索与分页，语义与 OpenAPI/文档一致。
4) **高度可用的随机图片 API**：`/random` 默认返回图片流，支持 `format=json`、`redirect=1`，并支持强筛选参数矩阵（`r18/r18_strict/orientation/min_width/min_height/min_pixels/included_tags/excluded_tags/user_id/illust_id/attempts/seed`）；遇到坏图/上游异常必须“换图重试”，不要直接把 404/5xx 甩给用户。
5) **Admin 可用且“看得懂”**：不停机热更新（导入/禁用/删除/补全/绑定/统计），深层 actions 必须可执行或明确跳转到可执行入口；空态/异常态要可解释且提供下一步路径。
6) **安全与可观测是硬门槛**：refreshToken/代理密码等必须 write-only + 全链路脱敏；/healthz 与 /version 必须可追溯 build commit；/metrics 默认不裸奔；所有关键动作可审计（request_id / jobId）。

当前仓库状态（本机）：
- repo：`pixiv-反代/pixivcat-backend`
- 分支：`test`
- HEAD：`6ab34451130a28b6179f95ab61cb7ece5179fe29`

---

## 2. “应实现能力”对照表（需求 → 实现点 → 现状 → 证据）

> 说明：本表的“线上现状”以 `docs/review/network/2026-02-09_05-56-38/*` 证据为准；如标记 `blocked:prod_redeploy_required` 表示代码已修复但线上未部署，无法复验。

| 能力 | 需求/验收点 | 关键实现位置（示例） | 现状（代码/线上） | 证据/测试 |
|---|---|---|---|---|
| 导入闭环 | 导入大量 URL 不 504；返回 import_id/job_id；可查进度/回滚；Image 记录增长 | `src/routes/adminImport.ts`、`src/jobs/importImages.ts`、`src/admin/pages/importUrls.jsx` | 代码：已实现；线上：旧部署表现为“看起来成功但不增长/不可追溯” | `test/admin_import.test.ts`；fix 说明：`docs/review/fixes/2026-02-09_05-56-38/DATA-0002.md` |
| 补全闭环 | refreshToken+proxy 冷路径补全 tags/authors/尺寸/R18；失败可重试可解释 | `src/jobs/hydrateMetadata.ts`、`src/services/pixivService.ts`、`src/routes/authors.ts` | 代码：已实现；线上：依赖真实 token/proxy + redeploy | `test/job_hydrate_metadata.test.ts`；`docs/review/fixes/2026-02-09_05-56-38/DATA-0001.md`（含 blocked） |
| 分类检索 | `/tags` `/authors` `/images` 可用 + 游标分页 + 与筛选参数一致 | `src/routes/tags.ts` `src/routes/authors.ts` `src/routes/images.ts` | 代码：已实现；线上：旧部署可能为空/契约不一致 | 证据（旧部署抽样）：`docs/review/network/2026-02-09_05-56-38/api-E4-tags-limit1.*`、`api-E5-authors-limit1.*` |
| /random 高可用 | image/json/redirect；attempts 换图；强筛选参数矩阵 | `src/routes/random.ts` `src/services/randomService.ts` | 代码：已实现；线上：旧部署抽样无 /version、契约可能不一致 | 证据（旧部署抽样）：`docs/review/network/2026-02-09_05-56-38/api-E6-random-json-sample.*`；`test/randomContract.test.ts` |
| Admin actions 深链可用 | `/admin/resources/*/records/:id/<action>` 可打开并触发（非 not found） | `src/admin/adminJs.ts`、`src/admin/components/recordActionRunner.jsx` | 代码：已修复；线上：旧部署仍 not found | 证据：`docs/review/network/2026-02-09_05-56-38/admin-A1*`、`admin-A2*`；fix：`docs/review/fixes/2026-02-09_05-56-38/ADM-0001.md` |
| Admin actions 不 504 | record/resource action 不同步阻塞；返回 jobId；可在 AdminJobs 追踪 | `src/jobs/adminActions.ts`、`src/admin/pages/adminJobs.jsx` | 代码：已修复；线上：旧部署仍可能 504 | 证据：`docs/review/network/2026-02-09_05-56-38/admin-A3*`；fix：`docs/review/fixes/2026-02-09_05-56-38/ADM-0002.md` |
| 安全（write-only+脱敏） | refreshToken/代理密码不回显；日志/审计/job output 无明文 | `src/admin/resources/pixivTokens.ts`、`src/admin/resources/proxyEndpoints.ts`、`src/audit/adminAudit.ts` | 代码：已修复；线上：旧部署仍泄露风险 | 证据：`docs/review/network/2026-02-09_05-56-38/admin-B1*`；测试：`test/admin_audit_redaction.test.ts` |
| /metrics 默认保护 | 默认不对公网裸奔；可配置 basic auth/显式匿名 | `src/routes/metrics.ts`、`src/config/env.ts` | 代码：已修复；线上：旧部署仍 200 | 证据：`docs/review/network/2026-02-09_05-56-38/api-E3-metrics.head.txt`；fix：`docs/review/fixes/2026-02-09_05-56-38/SEC-0002.md` |
| 可追溯部署 | `/healthz` + `/version` 返回 build.commit/version/build_time | `src/routes/healthz.ts`、`src/routes/version.ts`、`src/utils/buildInfo.ts` | 代码：已实现；线上：旧部署 /version 404 | 证据：`docs/review/network/2026-02-09_05-56-38/api-E1*`、`api-E2*`；fix：`docs/review/fixes/2026-02-09_05-56-38/DEP-0001.md` |
| UX 可解释/可恢复 | 空库/队列异常给出告警与下一步入口 | `src/admin/pages/dashboard.jsx`、`src/admin/pages/opsNavigator.jsx` | 代码：已补齐；线上：需 redeploy | 本轮提交：`[UX-2001]`（HEAD） |

---

## 3. 问题清单（按严重级别）

> 这里的“问题”以“线上当前部署”观测为准；若已在代码修复但线上未 redeploy，则标记为 `code_fixed / blocked:prod_redeploy_required`。

### High（业务不可用/安全红线/无法追溯）
1. **生产未 redeploy 导致“修复不可见”**：`/version` 404、`/healthz` 缺 build 字段、`/metrics` 仍匿名 200。  
   - 状态：`code_fixed / blocked:prod_redeploy_required`（必须先发布）
2. **Admin actions 大面积 deep link not found**（旧部署）：进入 record action 页面提示“资源没有该操作”。  
   - 状态：`code_fixed / blocked:prod_redeploy_required`
3. **Admin actions 同步触发导致 504**（旧部署）：Cloudflare 504 导致“看起来按钮不可用”。  
   - 状态：`code_fixed / blocked:prod_redeploy_required`
4. **敏感信息回显风险**（旧部署）：ProxyEndpoint 密码、PixivToken refreshToken 等存在明文回显/可读路径风险。  
   - 状态：`code_fixed / blocked:prod_redeploy_required`
5. **TokenProxyBinding 资源页 500（假可用）**：自定义页可用但资源页 500，导致管理员误判。  
   - 状态：`code_fixed / blocked:prod_redeploy_required`
6. **导入闭环不可追溯/不可恢复**（旧部署）：导入后无法确认是否入队/是否消费/进度如何。  
   - 状态：`code_fixed / blocked:prod_redeploy_required`

### Medium（体验误导/契约不一致/回归风险）
1. **Import UI 容易被 success=0 误导**：导入是异步入队，`success` 需要 job 完成后累计；应突出 `accepted/import_id/job_id/progress`。  
   - 状态：`code_fixed`（本地已修复文案 + 增加告警入口）
2. **tags 多值解析不一致风险**：重复 query/逗号/`|` 的兼容需契约化并测试。  
   - 状态：`code_fixed`（但需 prod 复验）
3. **legacy page=0 语义争议**：`/:illustId-0.ext` 易造成误解。  
   - 状态：`code_fixed`（已定为 301 到 page=1）
4. **/metrics 策略若配置不当仍可能暴露**：需要 Runbook 明确默认与建议配置。  
   - 状态：`code_fixed`（但需要部署侧执行）

### Low（可用性提升/引导/一致性）
1. **Dashboard/OpsNavigator 缺“下一步路径”**：空库/队列异常时用户不知道去哪里操作。  
   - 状态：`code_fixed`（本轮 UX-2001）

---

## 4. 关键问题明细（含证据链、代码定位、期望 vs 实际、修复建议）

> 为避免重复，本节以“问题簇”方式呈现，并给出对应 Issue/修复说明。每条均提供可复制复现步骤与证据文件路径（不含任何完整凭据）。

### 4.1 [HIGH] 生产未 redeploy：无法复验修复（最优先）

- 现象（2026-02-09 真实站点证据）：
  - `GET /healthz`：不包含 `build.*`  
    - 证据：`docs/review/network/2026-02-09_05-56-38/api-E1-healthz.body.txt`
  - `GET /version`：`404`  
    - 证据：`docs/review/network/2026-02-09_05-56-38/api-E2-version.headers.txt`
  - `GET /metrics`：匿名 `200`  
    - 证据：`docs/review/network/2026-02-09_05-56-38/api-E3-metrics.head.txt`
- 影响：
  - 你当前看到的“很多功能不可用”，无法区分是“代码没修”还是“没发布”。
  - 安全策略（/metrics、脱敏）无法在公网确认生效。
- 期望行为：
  - `/healthz` 与 `/version` 返回 `{ build: { version, commit, build_time } }`（无敏感信息）
  - `/metrics` 默认不对公网匿名开放（至少 basic auth 或显式 allowlist）
- 修复状态：
  - 已在代码实现（见 `docs/review/fixes/2026-02-09_05-56-38/DEP-0001.md`、`SEC-0002.md`），但 **prod 未 redeploy**。
- 修复建议（可执行）：
  1) 按 `docs/review/fixes/2026-02-09_05-56-38/DEP-0001.md` 做 redeploy 前 smoke：确保 build-info 写入、/version 路由可达
  2) 先跑迁移（`prisma migrate deploy`），再启动新镜像
  3) redeploy 后立刻执行：
     - `curl https://i.mukyu.ru/healthz`
     - `curl https://i.mukyu.ru/version`
     - `curl -I https://i.mukyu.ru/metrics`（确认不匿名）

### 4.2 [HIGH] Admin actions deep link not found（旧部署）→ “页面看起来能打开但核心动作不可用”

- 复现步骤（旧部署）：
  1) 登录 `/admin`
  2) 打开：
     - `/admin/resources/Image/records/1/hydrateMetadata`
     - `/admin/resources/PixivToken/records/1/testRefresh`
  3) 观察提示：资源没有该操作 / not found
- 证据链：
  - `docs/review/network/2026-02-09_05-56-38/admin-A1-image-hydrateMetadata-deeplink-notfound.summary.txt`
  - `docs/review/network/2026-02-09_05-56-38/admin-A2-pixivToken-testRefresh-deeplink-notfound.summary.txt`
  - screenshot：`docs/review/screenshots/2026-02-09_05-56-38/admin-A1-image-1-hydrateMetadata-deeplink-notfound.png`
- Root cause（已确认）：
  - AdminJS 的 `components.bundle.js` 缺少 `RecordActionRunner`，导致所有依赖该组件的 actions 深链页面无法渲染。
  - 证据：`docs/review/network/2026-02-09_05-56-38/admin-A0-components-bundle-missing-recordActionRunner.summary.txt`
- 代码定位（修复点）：
  - `src/admin/components/recordActionRunner.jsx:34`
  - `src/admin/adminJs.ts:363`（componentLoader 注册）
  - actions 引用：`src/admin/adminJs.ts:2388`、`:2448`、`:2538`
- 期望 vs 实际：
  - 期望：deep link 打开后渲染 action 组件并可触发
  - 实际（旧部署）：直接 not found
- 修复建议：
  - redeploy 后按 `docs/review/fixes/2026-02-09_05-56-38/ADM-0001.md` 复验；并在 AdminJobs 中检查 action 触发链路。

### 4.3 [HIGH] Admin actions 同步触发导致 504（旧部署）→ “点击按钮等死/假不可用”

- 复现步骤（旧部署）：
  1) 在 Image record 页触发 `hydrateMetadata`（或类似长动作）
  2) Cloudflare 返回 504
- 证据链：
  - `docs/review/network/2026-02-09_05-56-38/admin-A3-image-hydrateMetadata-post-504.summary.txt`
- 代码定位与修复方向：
  - 将长动作全部 **job 化**：handler 立即返回 `jobId`，不阻塞请求；并提供 AdminJobs 作为可观测/可恢复入口。
  - 详见 `docs/review/fixes/2026-02-09_05-56-38/ADM-0002.md`（含本地验证命令与线上复验步骤）。

### 4.4 [HIGH] 安全红线：refreshToken/代理密码回显与日志泄露风险（旧部署）

#### 4.4.1 ProxyEndpoint 密码回显（旧部署）
- 证据：
  - screenshot（已脱敏）：`docs/review/screenshots/2026-02-09_05-56-38/admin-B1-proxyEndpoint-1-edit-password-visible-masked.png`
  - network 摘要：`docs/review/network/2026-02-09_05-56-38/admin-B1-proxyEndpoint-password-visible.summary.txt`
- 修复状态与代码定位：
  - write-only + 全链路脱敏：见 `docs/review/fixes/2026-02-09_05-56-38/SEC-0001.md`

#### 4.4.2 PixivToken refreshToken 可读风险（旧部署）
- 风险描述：
  - 任何 Admin API list/show/edit GET 路径只要回显 refreshToken 明文，就属于不可接受的安全问题。
- 代码定位（已修复，write-only）：
  - `src/admin/resources/pixivTokens.ts:23`（GET 响应删除 refreshToken）
  - `src/admin/resources/pixivTokens.ts:94-109`（写入时规范化 + 只存 masked）
  - `src/admin/resources/pixivTokens.ts:168-181`（edit 响应清空/删除）
  - 测试：`test/pixivTokenResource.test.ts`、`test/admin_audit_redaction.test.ts`
- 线上复验（需 redeploy）：
  - 打开 PixivToken list/show，确认 UI 与 Admin API 响应均不含 refreshToken 明文。

### 4.5 [HIGH] TokenProxyBinding 资源页 500（假可用）

- 复现（旧部署）：
  - 打开 `/admin/resources/TokenProxyBinding` 直接 500
- 证据：
  - `docs/review/network/2026-02-09_05-56-38/admin-C1-tokenProxyBinding-resource-list-500.summary.txt`
  - screenshot：`docs/review/screenshots/2026-02-09_05-56-38/admin-C1-tokenProxyBinding-resource-list-500.png`
- 修复方向：
  - filter 值递归判空/解包，避免把空对象当 where 条件（详见 `docs/review/fixes/2026-02-09_05-56-38/ADM-0003.md`）。

### 4.6 [MED] Import “success=0”误导与闭环不可见

- 背景：
  - 导入已改为异步入队：**请求返回时 `success` 必然为 0**（尚未消费），正确理解应看 `accepted/import_id/job_id/progress`。
- 修复方向（UX）：
  - UI 必须把“已接收/入队”（accepted）与“处理完成累计”（success）区分，并提供进度刷新与回滚入口。
  - 空库/队列异常时，Dashboard/OpsNavigator 给出“下一步链接”。
- 本轮已补齐：
  - Dashboard/OpsNavigator 告警（Image=0 / queue 异常）：`src/admin/pages/dashboard.jsx`、`src/admin/pages/opsNavigator.jsx`（提交 `[UX-2001]`）

---

## 5. 必须立即修复 Top 10（按业务影响排序）

1) **Redeploy + migrate**（否则你看到的永远是旧行为；所有线上复验 blocked）  
2) **确认 /healthz 与 /version 可追溯 commit**（否则无法判定线上运行的到底是哪版）  
3) **/metrics 默认保护**（避免公网裸奔，尤其你现在还在迭代期）  
4) **Admin actions deep link 修复上线**（否则“页面可打开但动作不可用”的假可用会持续）  
5) **Admin actions 全部 job 化**（避免 504/卡死；必须可追溯 jobId）  
6) **ProxyEndpoint / PixivToken 全链路 write-only + 脱敏**（安全红线）  
7) **Import 闭环复验**：导入 100+ URL → Image 增长 → Import 进度 done=true → 可回滚  
8) **Hydration 闭环复验**：至少 3 个 illust 写入 tags/authors/R18/宽高 → `/authors` `/tags` 不为空  
9) **/random 契约矩阵复验**：image/json/redirect + attempts + seed + tags 多值解析一致  
10) **把“闭环入口”固定到 OpsNavigator**：减少你对复杂 Admin 资源页的依赖（先把“能用”跑通）

---

## 6. 可维护性 / 拓展性 / 健壮性风险（需要持续治理）

1) **外部依赖不确定性**：Pixiv 上游限流/封禁/验证码、代理稳定性不可控 → 必须持续投入退避/熔断/限速与可观测（失败原因要可解释）。  
2) **数据规模与随机查询性能**：随机挑选不能用 `ORDER BY random()`；需要索引/抽样策略与 benchmark（见 `docs/perf/*`）。  
3) **“看起来成功但其实没做事”风险**：所有长动作必须落到 job，可在 AdminJobs 查到，并能 retry/cancel。  
4) **敏感信息泄露风险**：任何新增字段/日志都可能绕过脱敏 → 必须保留并扩展脱敏单测（回归门槛）。  
5) **部署一致性风险**：没有 commit/build_time 暴露就无法排障 → `/healthz` `/version` 必须是发布门禁。

---

## 7. blocked 项（真实受限项）

- `blocked:prod_redeploy_required`
  - 说明：当前 `https://i.mukyu.ru` 证据显示仍为旧部署，无法对“已修复”功能做线上复验与回归关闭。
- `blocked:pixiv_upstream_or_proxy_required`
  - 说明：Hydration 元信息补全需要真实可用的 Pixiv refreshToken + 可用代理池；上游波动会导致验证不稳定，必须在可控窗口做复验。

---

## 8. 总结：当前版本是否达到“最初计划的完美实现”

以“线上当前部署”评价：**不达标**（大量关键问题仍可复现：/version 404、/metrics 200、Admin actions not found/504 等）。  
以“仓库 `test` 分支最新代码”评价：**核心闭环的修复已完成大半**（导入→入队→可追溯、actions 深链与 job 化、安全脱敏、/healthz+/version、/random 契约等均已落地并有测试），但最终是否“完美实现”的结论，必须在 **redeploy + migrate 后做一次全量 Chrome MCP 复验** 才能客观给出。

下一步执行合同（本轮）：
- `plan/2026-02-10_05-39-47-final-perfect-delivery.md`
- `issues/2026-02-10_05-39-47-final-perfect-delivery.csv`

